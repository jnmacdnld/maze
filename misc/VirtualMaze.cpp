VirtualMaze::VirtualMaze(VirtualMazeNode& maze_nodes) {
	
}

bitset<16> VirtualMaze::horizontalWallLineToBitset(string line) {
	bitset<16> wallBits;
	uint8_t wallIndex = 0;
	uint8_t lineIndex = 1;

	while ( lineIndex < line.length() && wallIndex < wallBits.size() ) {

	}
}